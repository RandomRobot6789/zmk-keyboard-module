if SHIELD_ALUMINUM_RIGHT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "AluminumKeyboard"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_ALUMINUM_LEFT || SHIELD_ALUMINUM_RIGHT

config ZMK_SPLIT
    default y

config SPI
    default y

# Needed for status_leds peripheral HID indicator events
config ZMK_HID_INDICATORS_CHANGED
    default y

endif
